---
import BaseLayout from '#layouts/Base.astro'
import Link from 'astro-typesafe-routes/link'
import { getCollection } from 'astro:content'

const entries = await getCollection('colloques')
---

<BaseLayout>
	{
		entries.map((itm) => (
			<Link to="/recherche/colloques/[slug]" params={{ slug: itm.id }}>
				{itm.data.title}
			</Link>
		))
	}
</BaseLayout>
